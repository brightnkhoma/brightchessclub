{
  "type": "module",
  "scripts": {
    "dev": "nodemon api/index.js",
    "start": "node api/index.js",
    "install:chess": "npm install --prefix chess",
    "build:chess": "npm run install:chess && npm run build:chess",
    "build:api": "cd api && npm install && npm run build",
    "build": "npm run build:chess && npm run build:api"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.2",
    "socket.io": "^4.7.3",
    "sqlite3": "^5.1.7"
  }
}
